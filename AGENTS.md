## Agent skills

### Issue 跟踪器

本仓库使用 GitHub Issues，所有操作通过 `gh` CLI。见 `docs/agents/issue-tracker.md`。

### Triage 标签

采用默认类别和状态标签。见 `docs/agents/triage-labels.md`。

### Domain 领域文档

根目录 `CONTEXT.md` + `docs/adr/`。见 `docs/agents/domain.md`。

### Repo Wiki（人类阅读）

`docs/repo-wiki/` 供人查阅；workflow skills 不读取。见 `docs/agents/wiki.md`。
