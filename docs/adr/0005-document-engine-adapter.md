# 文档引擎适配器

产品通过统一的 `DocumentEngine` 适配器接入 AIOffice/OfficeCLI 等第三方引擎，而不是在产品层直接绑定 OOXML 或某个引擎的细节。因为文件兼容性是高频变化且难以逆转的领域，适配器可以隔离引擎选型变化、格式能力差异和渲染/校验差异，使 HostAgent/AppAgent 只依赖稳定的结构化文件接口。最终选择哪个引擎，须以 Windows 黄金文件验证结果为准，不在本决策中提前锁死。
